# 动物档案 OCR 识别拆分工具 —— 项目交接文档

> 交接日期：2026-08-25 ｜ 版本：v1.1.3 结构判定与人工核对闭环（清单页页内结构判定 / 花名册字母簇优先 / 预览内手动拆分 / 花名册规模可视化）
> 上一版：2026-08-24 ｜ v1.1.2 交付健壮性加固（全局错误条 / 完整性自检 / 预览懒加载 / 免 MOTW 导出 / 1DAM 双保险）
> 本文档写给在 VS Code 上接手的 AI 编程助手（Kimi Code）或开发者。请先通读"踩坑记录"再动代码——本项目大部分复杂度来自已经踩过的坑，重蹈覆辙的代价很高。

---

## 1. 项目是什么

浏览器本地运行的扫描版 PDF 动物档案处理工具，用于实验动物档案的自动化整理：

- **逐页 OCR**（PaddleOCR PP-OCRv4 mobile，ONNX + onnxruntime-web，纯 WASM）
- **按动物编号（耳号/纹身号）把多动物合集 PDF 拆分成单动物 PDF**
- **按识别结果重命名**文件
- **中英文双版档案按编号合并**为每只动物一个 PDF
- **合格证/发票/检疫证按单据编号命名**（`No.` 前缀）
- **导出 Excel 档案汇总表**（编号、性别、出生日期、父母编号、体重等）

**硬性合规约束：全程浏览器本地运行，文件不上传任何服务器；用户环境不能安装软件**，因此交付物是单文件离线 HTML（38.7MB，模型 base64 内联）。

## 2. 本地跑起来

```bash
cd app
npm install
npm run dev        # 开发预览
npm run build      # 产出 dist/
bash rebuild.sh    # 完整构建：dist/ + dist-offline/（单文件离线版）
```

- Node 20+。依赖见 package.json（React 19 + Vite 7 + Tailwind + shadcn/ui + pdfjs-dist + pdf-lib + JSZip + xlsx + onnxruntime-web@1.22.0）
- `rebuild.sh` 会复制项目到 /tmp/build-app 构建，再经 `tools/make-offline.mjs` 把 JS/CSS/模型 base64 内联成单个 HTML。**本机已适配**：SRC 取脚本所在目录；本机未独立安装 Node，找不到 npm 时自动回退 Kimi 自带运行时（`/c/Users/Administrator/AppData/Local/Programs/Kimi/resources/resources/runtime`，node 24 + npm 11）；已加 `pipefail`，构建失败不会再被管道吞掉
- 本机没有标准 Downloads 目录，浏览器下载在 `D:\Backup\Downloads`
- **日常使用（脱离 VS Code / 开发环境）**：`app/dist/index.html` 与 `app/dist-offline/index.html` 是同一个 41.9MB 单文件（JS/CSS/ONNX 模型/wasm/字典全部 base64 内联），**双击即用、离线可用、可随意复制分发**（U 盘/别的电脑都行，Edge/Chrome 均可）。项目根目录放了一份改名副本 `动物档案OCR工具.html`，每次 rebuild 后需手动重新复制覆盖

## 3. 关键文件

| 文件 | 作用 |
|---|---|
| `src/lib/paddleOcr.ts` | OCR 引擎：onnxruntime-web 会话管理、DB 文本检测 + 行识别、印章抑制、cls 倒置检测、后处理、垃圾行过滤 |
| `src/lib/ctc.ts` | CTC 贪心解码 + top-2 单字符替换候选（纯函数，harness 直测） |
| `src/lib/ocr.ts` | 信息提取：标题提取、编号多通道提取、花名册纠错（编辑距离+top-k 组合）、顺序推定、清单页检测、Excel 字段提取、文字层直通 |
| `src/lib/ocrWorker.ts` / `src/lib/ocrPool.ts` | Worker 线程协议（init/open/ocr/ocrIdCell/close）与并行调度池 |
| `src/lib/sexGlyph.ts` | ♀/♂ 性别符号像素级分类器 |
| `src/lib/pageCache.ts` | 断点续跑：页级 OCR 结果 IndexedDB 缓存（LRU 6 文件） |
| `src/App.tsx` | UI + 分段逻辑 + PDF 拆分/合并（pdf-lib）+ 中英交叉验证 + 命名 + ZIP/Excel 导出 |
| `src/assets/binData.ts` | 读取 `window.__BIN__`（离线版注入的模型数据），Web 版回退到 `public/models/` 拉取 |
| `assets-bin/` | det.onnx(4.7MB) / rec.onnx(10.8MB) / cls.onnx(1.0MB) / dict.txt / ort.wasm —— 离线版数据源 |
| `public/models/` `public/ort/` | Web 预览版模型与 WASM 运行时（与 assets-bin 内容重复，两者都必须保留） |
| `tools/make-offline.mjs` | 离线单文件打包器 |
| `rebuild.sh` | 一键构建脚本 |

## 4. 踩坑记录（不要重蹈）

### OCR 引擎层
1. **字典 off-by-one**：dict.txt 首行是 'blank' 占位，CTC 索引直接映射 `keys[idx]`（idx=0 即 blank 须过滤）。写成 `keys[idx-1]` 会导致全页乱码。
2. **WebGPU 后端精度问题**：onnxruntime-web 的 WebGPU/jsep 后端会导致检测框大量丢失（7 框 vs 正常 22 框）。**固定只用 WASM 后端**（`createSession` 的 providers 写死 `['wasm']`），单线程 `ort.env.wasm.numThreads = 1`。
3. **det limit 960 不能调高**：mobile 检测模型按 960 训练，调到 1568 会因框得分被稀释而漏行。已实测，勿再试。
4. **文本行在检测概率图上只有 5-8px 高**：几何过滤（横线/竖线剔除）必须在**原图尺度**做（`rh<20 && rw/rh>4`），在 det 尺度做会杀死所有文本行。
5. **wasmPaths 必须是绝对 URL**：相对路径会被解析到打包产物目录。离线版用 `window.__BIN__` 内嵌 WASM，Web 版用 `new URL(\`${BASE}ort/\`, location.href).href`。
6. **pdfjs 会 detach buffer**：传给 pdfjs 的 ArrayBuffer 会被转移，之后还要用就必须 `buf.slice(0)`。

### 提取逻辑层
7. **isGarbageLine 的豁免规则**：编号样式的纯字母数字串（`/^[A-Z0-9][-A-Z0-9]{3,}$/i` 且含数字）天然置信度低，不能被 conf<0.55 规则杀掉；含数字的字符串不受"单字符重复率>0.5"规则约束（真实编号 2291242 有四个 2）。
8. **cleanToken 的陷阱**：日期与编号粘连（'1M12-02-25…'）需在日期起始位置截断（阈值 index>=2）；页码范围（'230-258'，`/^\d+-\d+$/`）必须拒绝。
9. **印章行不能当标题**：extractTitle 跳过 `/专用章|公章|印章/` 行；无关键词、长度<4 的候选直接跳过（防碎片标题）。
10. **单据页（isFormDoc）跳过候选池和兜底编号**：申购单/接收记录/清单等按标题命名，不抓页面上任何编号（否则 IACUC 编号、表单号会被误当动物编号）。
11. **花名册机制**：≥5 个同前缀同位数编号成簇才认作花名册，用于纠错（编辑距离≤2 唯一匹配）和顺序推定（档案页与花名册严格等差才推定）。**一页命中 ≥3 个花名册编号 = 清单页（rosterPage），强制独立成段**——寄宿费清单/总清单页含全部编号，不并入任何动物段。
12. **fallback 编号加 `No.` 前缀后，applyRoster 比对前要先剥前缀**，否则会被误判"对不上花名册"而丢弃。
13. **标题提取的碎片防护**（2026-08-19 回归新增）：红章环文会被 OCR 成无关键词短碎片（如「生物技」被当成寄宿费清单页标题）；英文正文句子碎片（小写开头/含小写且句号结尾，如 "in assigning animals to study groups."）也曾被当标题。现有 guard：无关键词且长度<4 跳过、英文句碎片跳过（全大写标题末尾的噪声句号不误杀）、页码行（Page 1/第 N 页）跳过、数据行（剥掉数字标点后剩 <2 个文字）跳过、含 `#` 的列头行跳过、页面最顶行 ×1.5 加权、顶行内左偏加权（居左是标题、居中是单位名、居右是页码）。两个坑中坑：日期行降权正则必须限定 `(19|20)\d{2}` 年份前缀，否则订单号末尾的句号（`112276.`）会误伤全大写标题；KEYWORDS 已含「发货」保护「发货单」这类短标题。
14. **extractArchiveRow 必须锚定耳号行**（2026-08-19 回归新增，按用户贴的真实 OCR 文本重写）：出生日期/父母编号对每个 id 出现处开 8 行窗口试解析（窗口先按 发货/Ship Date 截断），加每页全文兜底，按完整度+可信度打分取最优，同分取先出现（中文页在前）。否则全文第一个日期常是发货日期——8 只比格犬批次曾 5/8 行出生日期错成 8-17-26。要点：耳号行没有固定结构（编号/性别/日期/父母可拆成多行任意顺序，页角有光杆编号水印行）；SIRE+DAM 常粘成一整串跟在日期后（如 12-29-2578330001933169）按均分拆，列头字母误读成前导数字（S→5：578330001933169）剥 1~2 位再均分；日期正则 `(?<![\d-])(\d{1,2}-\d{1,2}-\d{2})`（年位后允许粘数字，前面防从长串中间截）；日期后的数字串若等于编号本身要跳过（8-17-267050002 粘耳号）；Excel 不收录 `No.` 前缀的单据行。
15. **OCR 层误读锚定救不回**：如 7050001 的 DAM 7465823 被读成 87465823，属识别精度问题，留给第三组待办。

### 工具链层
16. **构建验证**：改完必须 `bash rebuild.sh` 跑通，并在浏览器实测至少一批真实文件。
17. 历史上某平台的 edit_file 工具对 App.tsx 偶发"成功但没落盘"——AI 接手方修改文件后**务必 grep 验证**改动确实写入。
18. **离线断言 harness**（2026-08-19 引入）：用 esbuild 把 `src/lib/ocr.ts` 打桩打包（pdfjs/paddleOcr/worker 全部 stub，只测纯函数），node 直接跑 extractTitle/extractArchiveRow/extractInfo 断言。2026-08-25 重建为 tests/test-batch.entry.ts（+ stub-build.mjs，21 条断言，覆盖 v1.1.3 清单页判定/字母簇优先/列头陷阱/骨架分支/自证豁免/犬纯数字名册回归，含用户批次页 2/5/6 真实 OCR 文本）；旧版 79 条断言脚本曾放 /tmp/regress 已随临时目录丢失，重建时以新版为准扩充。这批纯逻辑函数以后改动都该先过断言再 rebuild。

### 并行与性能层（2026-08-20 新增）
19. **OCR 走 Web Worker 池**（`src/lib/ocrWorker.ts` + `src/lib/ocrPool.ts`）：并行度按 `navigator.hardwareConcurrency/4` 自动定、3 路封顶（每路要吃模型+页面位图内存）。浏览器读不到系统 CPU 占用率，这是唯一能自动拿到的指标。要点：**`window.__BIN__` 只在主线程存在**，模型字节由主线程解码后经 postMessage(transfer) 分发给每个 Worker（binData.ts 的 setBinBytes/getBinBytes 通道）；**Vite 必须 `worker.format: 'iife'`**——es(module) Worker 在 file:// 下会被 Chromium 异步拒绝（曾按错误记忆写成 es，导致离线版全灭，见踩坑 32）；classic Worker 里 onnxruntime 的 dynamic import() 实测正常。OCR Worker 里嵌套 pdfjs inline worker（`?worker&inline`）在 Chromium 可用。
20. **GPU（WebGPU）提速不划算，别再试**：① WebGPU EP 需要 jsep 运行时（`ort-wasm-simd-threaded.jsep.wasm`，22MB），离线单文件内嵌后从 40MB 涨到约 70MB；② WebGPU 后端数值精度不足曾致检测框大量丢失（踩坑 #2）；③ rec 的负载是一堆 48px 高小条图，GPU 调度开销占比大，小算子提速有限。WASM 多线程需要 SharedArrayBuffer + COOP/COEP，file:// 离线单文件拿不到跨域隔离，不可行。
21. **rec 批量推理（batch 合并调用）A/B 验证失败，已回滚**：理论只省调度开销，但实测 batched 卷积的数值差异会让低置信字符 argmax 翻转——40 裁片 A/B 中有 1~2 个真实字符翻转，**包括数字编号**（i030153390→i0301633928），限宽度比分组也不能稳定消除。而调度开销只占整页 10~15%。精度命脉换 10% 速度不值。验证脚本方法见第 7 节。

### 提取逻辑层（续）
22. **单据编号兜底**（2026-08-20）：省合格证/检疫证明/检测报告的编号写法是右上角**裸 `No.xxx`/`№ xxx`**、`流水号：xxx`、`报告编号：`（值居中写在下一行）。FALLBACK_LABELS 已加这些标签，且兜底取值不限同一行（右格 → 正下方格 → 下方 3 行 belowLineValue）。关键坑：检测报告页若走候选池（pool）抓到 RA0000-000001，会被花名册机制丢弃（pool 来源对不上花名册），而 `No.` 前缀的兜底编号会被保留——所以报告/证明/合格证/检疫类页面（isCertDoc）**跳过候选池**，让编号走兜底通道。标题可能带 OCR 空格（「检 测 报 告」），isFormDoc/isCertDoc/isArchive 判断前要先剥空格（titleFlat）。
23. **猴/猪档案字段用标签驱动提取**：犬档案（某供应商）字段是耳号行粘连数字串（两位年日期），但食蟹猴是 `Birth Date出生日期`/`Father No.父号`/`Mother No.母号`、巴马猪是 `出生日期`/`父亲编号`/`母亲编号` 标签版式，日期是四位年（2020-03-28）、性别是 ♀/♂ 符号——犬版启发式全抓空。det 按行序排列，同一视觉行的值格紧跟标签格，所以 extractArchiveRow 里按「标签行 → 紧邻下一行取值」解析（nextMatch），token 必须含数字（防 father/Mother 英文词被当值）。
24. **体重取值**：「体重/Weight」标签后的数字可能同行粘连也可能在下一行，两种都要收；值后禁止紧跟数字/小数点/短横（`(?![\d.-])`），否则相邻日期行 12-31-24 会被抓成 12。取文本序最后一次称重。
25. **花名册纠错会纠错**（2026-08-20 实测）：8340002 的档案页 OCR 误读后编辑距离 ≤2 唯一匹配到 8340001，档案被并进 8340001.pdf。机制本身是对的（低可信必标「待核对」高亮），但纠错结果必须人工抽查——这正是可疑项置顶的意义。
26. **debug 文本就是提取输入，绝不能截断**（2026-08-20 实测）：App.tsx 曾把每页 debug 文本 `lines.slice(0, 60)` 截断展示，而 extractArchiveRow 读的正是这份 debug——8320001 的最后一次称重 8.9 在第 70 行之后，被截没了，Excel 写成 8.5。现 debug 全量保留；改 App.tsx 时别再给 debugPages 加截断。
27. **兜底标签顺序就是正确性**（2026-08-20 实测）：某省合格证同一页有「实验单位使用许可证编号」（值 0002）和右上角裸「No.」行（值在下一行，40000000000001）。FALLBACK_LABELS 按序短路，通用「编号」标签若排在裸 No. 前会先命中许可证编号。规则：**越具体的标签排越前**；通用「编号」已加 `(?<!许可证)` 等排除；独立成行的裸 No./№ 用整行匹配模式 `/^\s*(?:No|№)\s*[.:：]?\s*$/i`，值交给 rightCellValue/belowCellValue。
28. **♀/♂ 性别符号在 OCR 层基本不可读**（2026-08-20 实测 8 页猪档案）：rec 模型对 ♀ 要么整格丢弃（7/8 页，连字符都不出），要么误读成「早」（conf 0.35）。已做两层补救：sexL 路径把「早」映射为 F；仍读不出时性别留空 + Excel 备注写「性别未识别」+ 该动物自动标「待核对」置顶。不要试图在文本层继续捞——需要人工补录，UI 已把这类行顶到最前。
29. **性别格图像级分类**（2026-08-20，`src/lib/sexGlyph.ts`）：♀/♂ 不在 rec 字典里，**加字典不可行**（CTC 输出层维度训练时固定，加字符必须重训模型）。但 **det 能框住符号**（实测 8/8 页都有 ~50×56 的 det 框，是 rec 把框丢了），所以在 recognizeCanvasPaddle 循环里收集所有过尺寸过滤的 det 矩形（allRects），循环后找「性别/Sex」标签右格裁片做像素分类：① 封闭圆孔（洪泛法）挡掉字母 M/F；② 左右镜像对称度 ≥0.87 判 ♀ 候选（真实 ♀ 0.895~0.985，渲染 ♂ 0.52~0.86；只算上半部反而拉不开——扫描倾斜影响更大）；③ 方向看区域密度：底部中央 > 右上 = ♀ 的十字，右上 ≫ 底部 = ♂ 的箭头。分类命中则替换「早」行或注入新行（text=M/F），判不出就留空走待核对。**绝不瞎猜**。阈值标定脚本 `/tmp/regress/pig_sex_sym.py`，8 页真实 + 12 渲染对照全对。
30. **双语标签的英文余韵会拆行/粘行**（2026-08-20 实测猪档案 p6/p7 父亲编号丢失）：「父亲编号/ID of the father」这种双语标签，英文部分常被拆成独立碎片行（thefather）或与值粘行（2230660thefather）。nextMatch 曾「下一行不匹配就 break」，碎片行一挡就丢值。现规则：① 纯英文碎片行（`/^[A-Za-z\s]{1,20}$/`）跳过继续找；② FULL_TOKEN 失败时用行内嵌 token 兜底（TOKEN_IN_LINE **不带 /i**——带 /i 会把 thefa 字母吸进 token；不带则小写碎片自然成为 token 边界，顺带修了「IDof2230660」粘连取成 IDof2230660 的隐患）；③ 值整行被 rec 丢掉时留空是正确的，不串取别人的值。另：关键字段（性别/出生日期/父母编号/体重）任一空缺 → Excel 备注逐个点名 + 该动物自动标「待核对」。
31. **C-LOT 接收档案是第三种猴版式 + 手写符号**（2026-08-20 实测）：「实验猴个体档案」用 父号：/母号：/性别： 独立标签行、值在右侧格。两个教训：① 体重正则的分隔符类必须含**全角冒号** `：`，「体重：\n3.30」曾因此漏抓；② 该版式性别格是**手写 ♂**（圆圈不闭合，孔洞检测漏过）——分类器返回 null 是正确行为，留空+待核对，不要为单个手写样本放松阈值误伤印刷体。性别符号分类器的标签匹配已扩展支持独立「性别：」行（印刷符号的该类版式可自动识别）。**排障工具**：ZIP 里附带 `_OCR原文汇总.txt`（每段完整 OCR 文本），浏览器端与 Python 复现管线的渲染差异（pdfjs vs pymupdf）会导致个别行粘连/丢失，字段对不上时先看它。
32. **file:// 下 blob: module Worker 被异步拒绝——离线版曾因此全灭**（2026-08-20 定位）：用户把 html 从 zip 里双击（解压到 Temp）或任何 file:// 直开，点开始处理永远卡「打开文件 · 2%」，console 报 `Refused to cross-origin redirects of the top-level worker script` ×N。有头 Edge CDP 探针实测（与路径/Temp/MOTW 无关，file:// 通病）：**blob: classic Worker ✓、blob: module Worker ✗（异步 error 事件）、data: classic/module ✓ 但 data: URL 超过 ~2MB 同样异步失败**。Vite `?worker&inline` 的引导是「先 blob:，同步抛错才退 data:」——blob module 的失败是异步的，兜底永远走不到。修法：`vite.config.ts` 的 `worker.format` 改回 `'iife'`（classic）——classic blob Worker 在 file:// 正常、无 2MB 上限，且 classic 脚本里 dynamic import() 依然可用（ORT 在 Worker 内加载 wasm 工厂不受影响，e2e 实证；踩坑 19 旧说法作废）。**附带加固**：ocrPool 之前完全没有监听 Worker 的 error 事件，Worker 一死所有 Promise 永久悬置（卡死的直接观感）；现 onerror 会摘除死亡 Worker、reject 挂在它身上的调用、全灭时置 broken 并给出明确错误文案，getOcrPool 下次调用自动重建。**验证方法**：CDP 端到端（`/tmp/regress/cdp_e2e.mjs`：有头 Edge + `DOM.setFileInputFiles` 喂真实 PDF + 轮询页面文本），file:// Temp 副本跑猪档案 8/8 拆出、console 0 错误。改 Worker 相关构建配置后必须重跑这个 e2e，别再只看构建通过。

### 构建与资源层（2026-08-21 新增）
33. **rebuild.sh 的 `cp -r` 嵌套坑**：`cp -r $SRC/tools $BUILD/tools` 在目标已存在时会嵌套成 `tools/tools`，旧 make-offline.mjs 残留导致**打包脚本改动静默不生效**（表现为 html 里缺新注入的资源键，文件大小不变是最快信号）。已对 tools 先 `rm -rf` 再拷。改 make-offline.sh/binData 链路后，先 grep 产物确认新字段在 html 里。
34. **新增模型/资源要穿透四层**：`assets-bin/`（源）→ `tools/make-offline.mjs`（__BIN__ 注入）→ `src/assets/binData.ts`（访问层 + BinBytes 类型）→ `ocrPool`（base64 解码 + transfer 给 Worker）→ `paddleOcr.initEngine`（建 session）；网页版还要 `public/models/` 兜底。断任何一层，Worker 里静默拿不到模型。
35. **合格证二维码直通：实测不可行，已否决**（2026-08-21）：江苏省实验动物质量合格证左下角确有二维码，但扫描件上仅 ~150px 宽、模块边缘洇开。zxing-js（MultiFormat/QRCodeReader × Hybrid/GlobalHistogram 两种二值化 × 整页/区域裁剪）与 OpenCV QRCodeDetector（原图/放大 2-4×/多档阈值/Otsu 共 9 种变体）全部解不出（最好成绩：检出定位点但解码为空）。前提是「100% 准确」，源不可解码则功能是死重。探针脚本 `/tmp/regress/qr_probe.py` + `qrprobe/probe2.mjs`。
36. **en_PP-OCRv5_mobile_rec 英文行路由：A/B 后否决**（2026-08-21，脚本 `/tmp/regress/ink_route_probe.py` / `enrec_ab.py`）：① 行前路由不可行——墨迹密度（48px 白底裁片暗像素占比）中英严重重叠：纯英文行 0.062~0.373（大写标题行 DESCRIPTION=0.373），含中文行 0.103 起（出生日期/Dateofbirth），没有可用阈值；② 「v4 输出纯 ASCII 且 conf<0.98 则 v5 复跑取高置信」的门控方案实测 +9/-2：v5 能修尾巴垃圾（`5-28-26[`→`5-28-26`），但也会把对的改错（`6-17-26`→`G-17-26`，`29.93`→`29.92`）；③ 关键认识：v5 修掉的边沿垃圾下游解析（DATE_RE/tokenFromText 的前缀匹配）本来就免疫，字段级收益≈0，却要多带 7.9MB 模型（离线包 +10.8MB）。**结论：不接。** v4 对数字类字段（编号/日期/体重）置信度已 ≈1.0，真正的精度瓶颈从来不是英文行。
37. **PP-OCRv5_mobile_det 亦否决**（2026-08-21）：实测比 v4 慢 22%（121→147ms/页），唯一框差异在圆形印章区（v4 碎 10 框、v5 并 1 大框），而印章已由红像素抑制处理（下条）——净收益为零。
38. **红色印章用像素级抑制而非通道变换**（2026-08-21，`paddleOcr.ts suppressRedSeal`）：`r>130 且 r-g>45 且 r-b>45` 置白，det 与 rec 裁片统一用处理后图。Python A/B（`ab_seal.py`）：猪页红像素 0%（无风险）；犬页 0.6~0.7%，章环文垃圾行清除、章下英文行读得更全、无关键编号行丢失。阈值保守，暗红/棕色字迹（整体暗）不会误伤。
39. **CTC top-k 候选纠错**（2026-08-21，`lib/ctc.ts`）：ctcDecode 记录每个已发射字符位的 top2，概率比 ≥0.25 的分歧位取 top-2 各生成一个单字符替换串（alts）；只有编号形态的行挂 alts（控制传输量）；alts 进 extractInfo 的 idCandidates；applyRoster 在编辑距离判不出唯一时，候选池**唯一精确命中**花名册才采用（hits.length===1 硬门槛，防清单页/串号误救）。合成张量断言 + e2e 双验证。
40. **cls 方向分类处理倒置页**（2026-08-21）：PP-LCNet_x0_25 cls（1.0MB，静态 [B,3,80,160]，输出 [B,2]）。recognizeCanvasPaddle 里 det 后取**最高的 8 行**裁片（文字行比碎片高，判定最稳）投票，≥3 票且过半、置信 >0.9 判倒置 → 整页旋转 180° 重跑 det+rec。预处理跟 RapidOCR：保宽高比到 H=80、右侧中灰补零到 160、[-1:1] 归一化。正常页开销 ≤8 次 ~1ms 推理。合成倒置猪档案 e2e 实证 8/8。注意 cls onnx 输出名是 `fetch_name_0`，按位置取。
41. **断点续跑缓存**（2026-08-21，`lib/pageCache.ts`）：页级 OCR 最终结果（debug+info）存 IndexedDB，key=文件名+大小+修改时间。三条铁律：① 缓存永不阻塞主流程（隐私模式/配额满静默降级）；② 只存页级结果，花名册纠错/推定是文档级后处理每跑重算；③ 失败页**不缓存**（可能是 Worker 瞬时错误，值得真重试）。LRU 保留最近 6 个文件。同一文件二次拖入全缓存命中时免 OCR 直接拆分（e2e 实证二跑秒出）。**提取逻辑升级时必须 bump `indexedDB.open(DB_NAME, 3)` 版本号清空旧缓存**——旧缓存页缺新字段（idAlts/idEvidence/listPage），命中后会绕过新逻辑悄悄复现旧 bug。
42. **花名册页 OCR 漏读 → 编辑距离纠错合并两只动物**（2026-08-21 深夜，v1.1.1 核心修复）：犬批次发货清单页漏读 8343597/8343830 → 花名册缺这两只 → 它们档案页标签通道提取正确（TATTOO 格+页角水印双现自证），却被 fuzzyMatch 编辑距离 ≤2「纠错」成相邻真实编号（8343830→8343083、8343597→8343512，增删各算一步恰好距离 2）→ 页面并入别只动物。修法三层：① **名册扩充**——标签来源 + 页内自证 ≥2 次 + 与名册同形态（字母前缀#位数）的编号直接入册不再纠错；② **纯数字密集编号簇纠错阈值收紧到距离 1**（7 位连续编号距离 2 的邻居极可能是另一只动物）；③ 纠错全失败时标签来源编号标 `offRoster` 保留（UI 琥珀徽标「编号不在花名册」），**绝不并入别只**。
43. **检疫证编号页顶裸编号无标签**（2026-08-21）：证号印在页顶、无「编号」标签、出现两次（条码数字+印刷体，如犬批 1101260850、猴 C-LOT 批 4600419780，OCR 会把标题误读成「动物柜寝合各证明」之类）。FALLBACK_LABELS 全需标签所以抓不到。修法：isCertDoc 页加步骤 5——整行 `/^(?:N[oO]?|№)?\s*[:.:：]?\s*(\d{8,20})$/` 匹配，排除手机号 `1[3-9]\d{9}`，要求全页 ≥2 次且至少一次在页顶 8% 内 → `No.{证号}`。限定 isCertDoc 防档案页整本同号错并。
44. **TATTOO 表头 O→0 误读击穿标签通道**（2026-08-21）：多页表头被读成 `TATTO0`/`TATT00`，`/Tattoo/i` 不匹配 → 标签通道失效退化为候选池乱抓。所有 `/Tattoo/i` 改 `/TATT[O0]{2}/i`（ID_CELL_LABEL + ID_LABELS.dog/generic）。
45. **猴档案双编号版式：序号 vs 原猴号**（2026-08-21）：猴页有两个编号——`OrdinalNumber序号`=027（批次工作编号，花名册=001-078）和 `MonkeyNo.原猴号`=A1902725。monkey 标签列表原猴号排第一 → 部分页命名成 A 编号破坏批次一致性。修法 `idAlts` 次级候选：主编号确定后把所有标签（含 `/序\s*号/`）的值收进 idAlts；applyRoster top-k 救援从「hits 唯一才救」放宽为「hits 与 idAlts 交集唯一也救」（实测 p50 候选池 [027,022] 双命中，序号标签值 027 交叉验证救回）。又一坑：猴名册页恰好漏读 052/078，这两页主编号取的就是原猴号 → 名册扩充同样吃进同形态 idAlts（052/078 入册后救援生效）。78 页 harness 全量验证 001-078 连续无缺。

### 交付与健壮性层（2026-08-24 新增，v1.1.2）
46. **file:// 下用户没有控制台：全局错误条是唯一故障反馈通道**：`index.html` head 内联脚本把 `error`/`unhandledrejection` 事件渲染成页面底部红条 `#__errbar`，并暴露 `window.__showErr(msg)` 供任何代码调起。用户报障只需截图红条，不用再教开 DevTools。
47. **41.9MB 单文件下载可能字节截断且无感知 → 构建期埋 FNV-1a 完整性自检**：用户曾拿到截断的 html，双击后 Worker blob 语法错误、页面「完全没反应」，排查极耗轮次。`tools/make-offline.mjs` 打包时计算主 JS 与 `__BIN__` 各字段（det/rec/wasm/dict/cls）的 FNV-1a 32 位哈希（对 UTF-16 码元，格式 `hex:length`，含长度可抓截断/追加两类损坏），runtime 500ms 后复算比对，不一致即红条「文件已损坏……请重新下载本工具」。
48. **导出文件被 Windows 标「文件有害」= MOTW，不是病毒**：`<a download>` 落盘的文件带 Zone.Identifier（网络来源标记），资源管理器/SmartScreen 拦截预览。修法：保存统一走 `saveBlob()`，优先 `showSaveFilePicker`（File System Access API）——用户亲手选路径写出的文件不带 MOTW；用户取消（AbortError）静默不存；不支持的环境回退普通下载。Excel/PDF/ZIP 三个出口全覆盖。
49. **结果预览必须懒加载**：中英合并后单只动物多页，整本全渲 canvas 打开即卡。改为首屏只渲第 1 页（1200px 上限），其余页放占位块，IntersectionObserver（rootMargin 600px）滚动近屏才渲；渲染走串行 Promise 链防并发吃爆内存；关闭时 `io.disconnect()` + `doc.destroy()`。
50. **英文碎片防混编号要双保险**：英文表头碎片（如 `1DAM`）会被候选池当编号产出 `1DAM.pdf`。只在候选池循环里 skip 不够——`cleanToken` 出口也要同闸（含字母但数字占比 <0.5 返回 null），两条路径都堵才根治。
51. **e2e 批处理完成信号不是「已拆出」**：「已拆出 N 个文件」是**单文件**消息，多文件批处理听到第一个就动手会读到半成品列表（曾误判「翻译件被吞」，实际还在跑）。正确信号：开始处理按钮的「处理中…」文案消失。headless CDP 回归必须起独立 `--user-data-dir`，否则断点续跑缓存跨运行残留，计时和覆盖率都是假的。
52. **编辑会静默丢改，grep 复核是唯一防线**：并行 edit 同一文件互相覆盖，串行 edit 也曾整段丢失（本会话连丢 5 次：版本戳/saveBlob 定义/懒加载/cleanToken 闸/Excel 的 saveBlob 出口——最后这个直到推送前逐段通读才发现）。铁律：同文件编辑串行执行；**每次构建前 grep 复核全部改动点**（当前 checklist：`滚动至此自动加载`/`构建版本`/`saveBlob`×4（定义+PDF/ZIP/Excel 三个出口）/`数字占比`×2/`__showErr`/自检脚本注入/`listPage` ocr.ts 6 处+App.tsx 3 处/`splitOutput`/`bestLetter`/IndexedDB 版本号），推送前再 diff 上游逐行过一遍。另：**`npx tsc --noEmit` 对 references 型 tsconfig 是假绿**（什么文件都没检查也退 0），类型检查只信 `npm run build` 里的 `tsc -b`——v1.1.3 曾因此漏掉 applyRoster 一个裸 return（声明返回 number），构建才暴露。

### 结构判定与人工核对层（2026-08-25 新增，v1.1.3）
53. **花名册会被测量值大簇挤掉 + 清单页检测不能依赖花名册先验**（南京药明 27 头巴马猪批次实测）：① 抗体检测表的 OD 值被剥成 3~4 位纯数字凑出 ~50 个大簇，buildRoster「数量优先、同数量才偏字母」的规则让 OD 值簇挤掉 26 个 B 编号名册 → 全批档案页标「编号不在花名册」；② 合格证动物清单页的列头「耳号No.ofear」命中猪编号标签正则，belowCellValue 取到首行数据 B265402 → 清单页 2-4 抢注了 B265402，与档案封面（页 6）被合并成一个文件。修法即「人眼式」结构判定（extractInfo 内，不靠标题文字）：**页内同形态编号簇 ≥5 → 清单页（listPage）**；簇 3~4 时需同构行骨架（数字→#/字母→A/中文→中）重复 ≥5 佐证（OCR 读烂大半编号时的救援）；**单主体签名豁免**——标签编号页内双现自证（evidence≥2）的页永不判清单页（档案页内的疫苗记录表就是同构行，不能误杀）。listPage 不信任 label/filename/pool 三通道（No. 单据兜底保留），分段强制独立成段。另有**列头陷阱补网**：簇 ≥4 且标签取值无自证（evidence≤1）→ 作废（小批次清单页簇不满 5 时兜底）。花名册选簇改**字母前缀簇绝对优先**（≥5 即成册），纯数字簇（犬 7050001 型）只在没有字母簇时启用。applyRoster 返回名册规模，批次摘要显示「花名册 N 个编号」供人工确认。harness 21 条断言（tests/test-batch.entry.ts，含本批页 2/5/6 真实 OCR 文本复现）。
54. **核对闭环要在预览里完成**：用户核对时看到混页，只能重跑不能改 = 不可用。预览弹窗每两页之间有「✂ 在此拆分」（pdf-lib 物理拆页：前半保留原名与编号，后半无编号、标待核对置顶、自动命名 `原名_第x页起` 可立即改名）；合并文件（mergedCount>1）预览顶部有「取消合并」拆回各来源段（实现：mergedFrom 记录来源段 id + unmergedIds 集合让合并 effect 跳过，拆分只作用于 outputs 原段，合并视图自动跟随）。**info 结构加字段必须 bump IndexedDB 版本**（现 v3）：旧缓存页缺 listPage 会绕过新判定复现旧 bug（踩坑 41 铁律）。

## 5. 当前已完成功能

- PP-OCRv4 mobile 中英识别、多通道编号提取（标签 > 文件名 > 候选池 > 兜底）
- **多 Worker 并行 OCR**（按 CPU 核数自动定并行度，3 路封顶；pdfjs 渲染 + OCR 全在 Worker 线程）
- 花名册纠错 + 顺序推定 + 清单页检测；**用户粘贴编号清单当外部花名册**（<5 只小批次救星，UI 折叠区粘贴）
- 按编号/按页拆分、中英文同编号合并（默认开启「合并相同编号」）
- 命名：动物编号 / 标题 / 合格证、检疫证明、检测报告、发票等单据 `No.{编号}`（含裸 No./№/流水号/报告编号版式）；**自定义命名模板**（`{编号}`/`{标题}`/`{来源}` 占位符，localStorage 记忆）
- 模糊页高清重试开关、**编号格放大 2.5× 三级重试**（顽固页）、处理历史记忆
- ZIP 打包下载、Excel 档案汇总表（动物编号/性别/出生日期/父母编号/最新体重/来源/页码/备注；犬/猴/猪三种档案版式均可提取）
- **结果列表手动改名**（行内编辑，下载/ZIP/Excel 均生效，可一键恢复自动命名）
- **可疑项高亮置顶**（顺序推定 / 花名册纠错 corrected / 低可信候选池 lowConf / 未识别 / 性别未识别 → 置顶 + 琥珀色高亮 + 「待核对」标签）+ **只看待核对筛选**
- **结果在线预览**（结果行点「预览」，直接从内存渲染 PDF 页面，无需下载；**懒加载**：首屏只渲第 1 页，滚动到才渲后续页，见踩坑 49）
- **全局错误红条**（file:// 下静默 JS/Worker 错误可视化，截图即可报障，见踩坑 46）
- **单文件完整性自检**（下载截断/损坏直接红条提示重新下载，见踩坑 47）
- **导出免 Windows 安全拦截**（另存为对话框写盘不带 MOTW 网络来源标记，见踩坑 48）
- **页脚构建版本戳**（报障时一眼确认用户手上是哪一版）
- **批次摘要**（结果标题下：本批 N 个文件、动物 X 只、单据 Y 个、未识别 Z 个、待核对数）
- 标题碎片防护 + Excel 字段耳号行锚定（见踩坑 13/14）
- **♀/♂ 性别符号图像级识别**（绕过 rec 字典限制：det 框定位性别格 + 孔洞/左右对称/区域密度分类，见踩坑 29）
- **PDF 原生文字层直通**（getTextContent ≥30 字符判定非扫描页，免渲染免 OCR，秒出）
- **红色印章像素抑制**（饱和红置白，章压字恢复可读，见踩坑 38）
- **中英文双版交叉验证**（合并时按 CJK 占比分组互查 性别/出生日期/父母编号/体重 五字段，不一致标红 + Excel 备注写差异明细）
- **CTC top-k × 花名册组合纠错**（rec 次优路径单字符替换候选入候选池，唯一精确命中才救，见踩坑 39）
- **180° 倒置扫描页自动转正**（cls 行级投票，见踩坑 40）
- **断点续跑**（页级结果 IndexedDB 缓存，中断后重拖同一文件只补剩余页；全命中免 OCR 直接拆，见踩坑 41）
- **花名册防误纠**（名册页漏读的编号凭标签自证扩充入册；纯数字簇纠错限距离 1；对不上名册的标签编号标「编号不在花名册」保留，绝不并入别只动物，见踩坑 42）
- **检疫证页顶裸编号提取**（无标签证号按「全页 ≥2 次 + 页顶出现」识别，命名 `No.{证号}`，见踩坑 43）
- **双编号版式交叉验证**（猴档案 序号/原猴号 双标签，idAlts 次级候选参与花名册救援，批次命名统一为序号，见踩坑 45）
- **清单/表格页结构判定**（页内编号簇 ≥5 或 簇3~4+同构行骨架重复 → listPage，不取动物编号、强制独立成段；单主体双现自证豁免；列头取值无自证作废，见踩坑 53）
- **花名册字母前缀簇优先**（测量值大簇不再挤掉动物编号名册）+ **批次摘要显示花名册规模**（人工一眼确认名册选对，见踩坑 53）
- **预览内手动拆分**（页间「✂ 在此拆分」物理拆页，后半自动命名+待核对）与**取消合并**（合并项一键拆回来源段），核对闭环不用重跑（见踩坑 54）

## 6. 待办清单（已与需求方讨论确认方向，按优先级）

**第一组 · 省人工（2026-08-19 已完成）**
- [x] 结果列表手动改名（文件名可编辑）
- [x] 低置信/推定/纠错过的结果高亮置顶
- [x] 批次摘要（"9 个文件，8 只全部识别，2 个推定需核对"）

**第二组 · 精度放大（2026-08-21 全组清零）**
- [x] 用户粘贴编号清单当花名册（解决 <5 只小批次花名册失效）（2026-08-21）
- [x] 命名模板自定义（`{编号}_{日期}` 等）（2026-08-21）
- [x] ~~页面缩略图悬停预览（核对命名用）~~ → 已由「结果在线预览」覆盖（2026-08-20）
- [x] 断点续跑（页级 IndexedDB 缓存，2026-08-21）；「大文件分片」由 Worker 池逐页派发天然成立，无需另行改造

**第三组 · OCR 精度（免换模型）（2026-08-21 全组清零）**
- [x] 红色印章滤除：实测改为**饱和红像素置白**（比取蓝色通道更保守，黑/蓝字迹零风险）（2026-08-21）
- [x] 中英文双版交叉验证：合并时按 CJK 占比分组互查五字段，不一致标红 + Excel 备注（2026-08-21）
- [x] CTC top-k 候选 × 花名册组合纠错（2026-08-21，见踩坑 39）
- [x] 编号单元格裁剪放大 2.5 倍重识别（三级重试，2026-08-21）
- [x] ~~合格证二维码直通~~ → **实测否决**：扫描件二维码 ~150px 模块洇开，zxing-js 与 OpenCV 全预处理变体均解不出（2026-08-21，见踩坑 35）
- [ ] 表格线检测切单元格（OpenCV.js）→ **评估后暂缓**：+8MB wasm 且要绕开现有行序逻辑，而近几轮真实批次的残余误差根因（性别符号/印章/单字符误读）均已由更小的方案解决，当前没有「粘死到正则分不开」的未决样本。触发条件：出现真实粘连故障样本再做
- [x] PDF 原生文字层直通（pdfjs getTextContent，非扫描页免 OCR）（2026-08-21）

**第四组 · 模型升级（需 A/B 验证）（2026-08-21 调研+实测完毕）**
- [x] ~~det 换 PP-OCRv5_mobile_det~~ → **A/B 否决**：慢 22%，唯一差异在印章区而印章已被像素抑制覆盖（见踩坑 37）
- [x] ~~英文行加 en_PP-OCRv5_mobile_rec 路由~~ → **A/B 否决**：墨迹密度不可路由（中英重叠无阈值）；门控复跑 +9/-2 但收益场景下游已免疫（见踩坑 36）
- [x] 加 cls 方向分类模型（PP-LCNet_x0_25，1.0MB），处理倒置 180° 扫描页（2026-08-21，见踩坑 40）
- [ ] 长期：合成数据微调 rec → **评估结论**：当前 rec 在字段级（编号/日期/体重数字）置信度已 ≈1.0，残余误读无系统性模式；微调需 PaddlePaddle 训练环境 + 合成数据生成器 + 转换链（paddle2onnx 已验证可通），预计 2-3 天换 <5% 字符级收益，性价比低。触发条件：出现固定模式的系统性误读（某字体固定把 X 读成 Y）时，先试官方 server 级模型再考虑自训

**明确不做的**：大模型路线（PaddleOCR-VL 等，浏览器跑不动且违反本地合规）；GLP 合规字段对齐/审计追踪（需求方明确只要"代替人工命名"）；GPU/WebGPU 加速与 rec 批量推理（2026-08-20 实测否定，见踩坑 20/21）。

## 7. 验证方法

- **标准测试集**：需求方手上的 8 只比格犬批次文件（中文翻译件 + 英文原件 + 质量合格证 + 检疫合格证 + 接收记录 + 申购单 + 发票）。每次改动后跑一遍，对照预期：8 个动物 PDF（编号 7050001/7050002/7050003/7050004/7050005/7060001/7060002/7060003）+ 清单页独立文件 + 单据按标题/No.命名
- **快速自检**：引擎暴露 `window.__paddleRecognize` 钩子，可在控制台对 canvas 冒烟测试
- **Python 复现管线**：可用 onnxruntime Python 版加载 assets-bin 里的模型离线复现 JS 管线（det→crop→rec），用于无浏览器调试
- **输出 PDF 内容核对**（2026-08-19 实践）：工作区根目录有 `.venv-tools/`（已装 pymupdf），可把输出 PDF 渲染成 PNG 直接看页面内容——不确定某个输出文件是什么时，渲染出来看比猜 OCR 文本快得多
- **纯逻辑离线断言**：见踩坑 18，extractTitle/extractArchiveRow/extractInfo 这类纯函数改动先在 node 下跑断言（tests/test-batch.entry.ts，当前 21 条：v1.1.3 结构判定系列 + 犬/猪名册回归；旧版 79 条覆盖犬粘连串、猴/猪标签版式、合格证裸 No.、检测报告下一行取值等用例，重建时可按第 4 节各踩坑的描述补回）
- **推理层 A/B 验证**（2026-08-20 实践）：改 onnx 推理调用方式（批量/后端/参数）前，用 `.venv-tools` 里的 Python onnxruntime 加载 `assets-bin/rec.onnx`，对同一批真实裁片跑「现状 vs 新方案」对比解码结果（脚本方法：渲染一页 PDF → 随机取文字行裁片 → 两种调用各跑一遍 → 逐条比对文本）。数值级差异（字符翻转）肉眼看不出来，必须这样量化对比

## 8. 给接手 AI 的工作方式建议

1. 改任何文件后 grep 验证落盘；改完 `bash rebuild.sh` 必须全绿
2. OCR 提取规则的每次改动都要用第 7 节的标准测试集回归——本项目无自动化测试，历史教训是"修一个新的、坏一个旧的"
3. 不确定的精度改动做 A/B 对比再合入
4. 大改后更新本文档
